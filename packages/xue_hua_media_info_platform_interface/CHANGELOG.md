# Changelog

## 2.0.0

Initial 2.0 platform interface: sealed `MediaSource` / `MediaMetadata` types,
Pigeon `MediaInfoHostApi`, and `MethodChannelXueHuaMediaInfo`.

2.0 平台接口初版：密封类型、Pigeon Host API 与默认 MethodChannel 实现。
