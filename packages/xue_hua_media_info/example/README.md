# xue_hua_media_info example

Demonstrates `MediaInfo.readImage` / `readAv` with bundled JPEG, PNG and MOV
samples under `assets/testdata/`.

演示门面 API：用 `assets/testdata/` 中的 JPEG、PNG、MOV 样例读取类型化元数据。
